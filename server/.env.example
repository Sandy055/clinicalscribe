# Copy this file to a new file named ".env" (same folder) and fill in your real values.
# The .env file holds secrets and should NEVER be committed to GitHub.

# Your Anthropic API key — get one at https://console.anthropic.com
ANTHROPIC_API_KEY=your_api_key_here

# The port the backend server runs on
PORT=3001
