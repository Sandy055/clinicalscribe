# Dependencies — these get reinstalled with "npm install", never commit them
node_modules/

# Secrets — your real API key lives in .env, which must NEVER be committed
.env

# Build output
dist/

# OS / editor junk
.DS_Store
